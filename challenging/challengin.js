function generateParen(num) {
  for (var i = 0; i < num; i++) {
    var arr = []
    for (var j = i; j < num; j++) {
      if () {
        arr.push('(')

      }
    }
  }
  return arr
}

console.log(generateParen(2));
